# Nexha OS Runtime — Standard Tier Override
# Standard = Lite + SUTAR OS + Network services (no monitoring)
# Run: docker compose -f docker-compose.yml -f docker-compose.standard.yml up -d

version: '3.9'

services:
  # Override: remove enterprise-only monitoring
  capability-os:    { profiles: [], restart: "" }
  acp-messaging:   { profiles: [], restart: "" }
  agent-marketplace: { profiles: [], restart: "" }
  mission-planner:  { profiles: [], restart: "" }
  prometheus:       { profiles: [], restart: "" }
  grafana:          { profiles: [], restart: "" }
