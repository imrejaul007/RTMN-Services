---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexha-intelligence
  namespace: nexha
  labels:
    app: nexha-intelligence
    tier: backend
    product: nexha
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nexha-intelligence
  template:
    metadata:
      labels:
        app: nexha-intelligence
        tier: backend
        product: nexha
    spec:
      containers:
      - name: intelligence
        image: nexha/intelligence:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 4000
          name: http
        envFrom:
        - configMapRef:
            name: nexha-config
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "4000"
        - name: SERVICE_NAME
          value: "nexha-intelligence"
        - name: MONGODB_URI
          valueFrom:
            secretKeyRef:
              name: nexha-secrets
              key: MONGODB_URI
              optional: true
        - name: AUTH_SERVICE_URL
          valueFrom:
            secretKeyRef:
              name: nexha-secrets
              key: AUTH_SERVICE_URL
        - name: INTERNAL_SERVICE_TOKEN
          valueFrom:
            secretKeyRef:
              name: nexha-secrets
              key: INTERNAL_SERVICE_TOKEN
        - name: WEBHOOK_SECRET
          valueFrom:
            secretKeyRef:
              name: nexha-secrets
              key: WEBHOOK_SECRET
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 4000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 4000
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /health
            port: 4000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 12
---
apiVersion: v1
kind: Service
metadata:
  name: nexha-intelligence
  namespace: nexha
  labels:
    app: nexha-intelligence
    product: nexha
spec:
  selector:
    app: nexha-intelligence
  ports:
  - port: 80
    targetPort: 4000
    name: http
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: nexha-intelligence-hpa
  namespace: nexha
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: nexha-intelligence
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
