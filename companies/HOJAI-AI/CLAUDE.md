# HOJAI-AI - Git Workflow

## ⚠️ CRITICAL

This is a **SUBMODULE** inside RTMN repo.

## Branch

**ALWAYS use `main` branch**

```bash
git checkout main
git add -A
git commit -m "feat: Add EmotionOS"
git push origin main
```

## Push Sequence

1. Push HOJAI-AI to `main`
2. Update RTMN submodule reference
