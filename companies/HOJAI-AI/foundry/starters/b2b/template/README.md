# {{PROJECT_TITLE}}

> Wholesale B2B platform with RFQ + quote + trade finance + escrow.
>
> Auto-generated by HOJAI Foundry using the `b2b` starter.

## Quick start

```bash
npm install
npm run dev
# → http://localhost:3000  (frontend)
# → http://localhost:4001  (backend)
```

## What's inside

- **Backend** (`apps/backend`) — Express on :4001
- **Frontend** (`apps/frontend`) — Static HTML + vanilla JS on :3000
- **Agents** (`apps/backend/src/agents/`) — 6 SUTAR agent stubs
- **Nexha** (`/api/nexha/profile`) — Federation profile
- **hojai.ai.md** — AI-native spec for Claude Code / Cursor / Codex

## Endpoints

- `/api/rfq`
- `/api/quote`
- `/api/order`
- `/api/admin`
- `/api/agents`
- `/api/nexha`

## Agents

- **CEO** — Orchestrator. Routes work across sales, procurement, finance, logistics.
- **Sales** — RFQ → quote conversion, deal tracking.
- **Procurement** — Supplier discovery, RFQ broadcast, price negotiation.
- **Finance** — Trade finance, escrow, BNPL, payment reconciliation.
- **Logistics** — Shipping quotes, customs, dispatch.
- **Support** — Buyer/seller tickets, dispute mediation.
