# Mobile — placeholder

When you're ready to add a mobile app, run:

```bash
npx hojai add mobile
```

This will scaffold a Flutter app (iOS + Android) under `apps/mobile/`, wired to the
same backend at `http://localhost:4001`.

For now, the web frontend at <http://localhost:3000> is fully functional and is
what the 30-minute demo walks you through.
