{
  "name": "@hojai/refresh-scheduler",
  "version": "1.0.0",
  "description": "Refresh Scheduler (5340) - schedule re-crawls, TTL-based refresh, on-demand",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test --test-force-exit --test-concurrency=1 tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}