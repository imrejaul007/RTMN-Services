# Agent SDK

**Port:** 4187
**Type:** Platform Intelligence Service (SDK Distribution)
**Package:** `@hojai/agent-sdk`

## Overview

TypeScript + Python SDK source packages for building agents on HOJAI AI. Includes the runtime, registry, type definitions, and helper libraries. Serves the published SDK source via HTTP for downstream consumers.