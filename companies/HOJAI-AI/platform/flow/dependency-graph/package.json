{
  "name": "@hojai/dependency-graph",
  "version": "1.0.0",
  "description": "Phase 14.2: Manages task dependency graphs. Stores DAGs, performs topological sort, detects cycles, computes parallel-ready batches, and persists state across restarts.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test tests/unit/"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "uuid": "^9.0.0"
  }
}
