{
  "name": "@hojai/mm-asset-store",
  "version": "1.0.0",
  "description": "Multi-Modal Asset Store (5343) - raw bytes + metadata + content hash dedup",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test --test-force-exit --test-concurrency=1 tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}