# Company Manifest Template
# Generated by CompanyOS Composition Engine

version: "1.0.0"
companyId: "{{company_id}}"
name: "{{company_name}}"
industry: "{{industry}}"
createdAt: "{{created_at}}"
updatedAt: "{{updated_at}}"

composition:
  departments: []
  extensions: []
  aiWorkers: []

twins:
  companyTwin:
    id: "{{company_twin_id}}"
    type: company
    service: twinos-hub
    endpoint: "http://localhost:4705"

  departmentTwins: []
  extensionTwins: []
  workerTwins: []

metadata:
  createdBy: composition-engine
  environment: "{{environment}}"
  region: "{{region}}"
  tags: []

checksum: "{{checksum}}"
