{
  "name": "@hojai/gpu-cluster-manager",
  "version": "1.0.0",
  "description": "HOJAI AI GPU Cluster Manager — register GPU nodes, allocate to jobs, monitor utilization",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test --test-force-exit --test-concurrency=1 tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}